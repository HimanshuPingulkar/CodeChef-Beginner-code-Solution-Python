from math import sqrt

t = int(input())
for _ in range(t):
    a = int(input())
    c = 0
    
    while True:
        b = int(sqrt(a))
        if b*b == a:
            c += 1
            break
        c += 1
        a = a - b*b
    print(c)
